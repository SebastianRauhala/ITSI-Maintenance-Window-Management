#   ITSI Maintenance Window Management - server.conf
#
# Ensure the app's custom configuration replicates across search-head cluster
# members so idempotency/safety settings stay consistent.

[shclustering]
conf_replication_include.itsi_maintenance = true
